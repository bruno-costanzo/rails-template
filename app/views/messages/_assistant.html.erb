<% assistant ||= local_assigns[:message] %>
<div id="message_<%= assistant.id %>" class="chat chat-start">
  <div class="chat-header">
    Assistant
    <time class="text-xs opacity-50"><%= assistant.created_at&.strftime("%I:%M %p") %></time>
  </div>
  <div id="message_<%= assistant.id %>_content" class="chat-bubble whitespace-pre-wrap"><%= assistant.content %></div>
</div>
